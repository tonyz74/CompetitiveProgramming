#include <iostream>

using namespace std;

void solve() {
    int c1, c2, c3, c4; cin >> c1 >> c2 >> c3 >> c4;

    if (abs(c2 - c1) >= 2) {
        printf("0\n");
        return;
    }

    
}

int main(void) {
    int t; cin >> t;
    while (t--) solve();
}