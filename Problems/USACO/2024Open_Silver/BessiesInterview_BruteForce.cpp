#include <iostream>
#include <vector>

using namespace std;

int N, K;
vector<int> times;

void dfs(int now) {
    
}

int main(void) {
    cin >> N >> K;
    times.resize(N + 1);

    for (int i = 1; i <= N; i++) {
        cin >> times[i];
    }
}